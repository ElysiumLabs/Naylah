namespace Naylah.App.IoC
{
    //public class ServiceProviderDependencyResolverAdapter : IDependencyResolver
    //{
    //    private readonly IServiceProvider serviceProvider;

    // public ServiceProviderDependencyResolverAdapter(IServiceProvider serviceProvider) {
    // this.serviceProvider = serviceProvider; }

    // public T GetService<T>() { return (T)serviceProvider.GetService(typeof(T)); }

    // public object GetService(Type serviceType) { return serviceProvider.GetService(serviceType); }

    // public IEnumerable<T> GetServices<T>() { serviceProvider. }

    //    public IEnumerable<object> GetServices(Type serviceType)
    //    {
    //        throw new NotImplementedException();
    //    }
    //}
}